import styles from "./Header.module.css";

const Header = () => {
    return (
        <header>
            <h1 className={styles.name}>slavagm</h1>
        </header>
    );
};

export default Header;